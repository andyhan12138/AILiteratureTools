<!-- ============================================================
EDIT ME(可选)—— 这是相关性标尺的【(b) 长期研究画像】部分,
只有在调用时加了 --use-interest 才会被用到。

survey-snowball 的相关性标尺由三块按需组合:
  (a) 本次调研临时给的 topic        —— 命令行 --topic "…"(最贴合"调研某个思路")
  (b) 这份长期研究画像              —— 命令行 --use-interest 才并入
  (c) 种子论文自身主题             —— 默认就会用(--no-seed 可关)

如果你也在用 arxiv-scan,可以直接把那份 interest.md 的内容复制过来,
或调用时用 --interest-file 指向它,免得维护两份。

下面整段(含本注释)会被自动忽略,直到你写入 ≥30 字的实质内容。
============================================================ -->

<!-- 在此用一段话描述你的长期研究方向(对象、方法、关心什么、明确不关心什么)。
例:我做 <子领域> 的 <理论/数值/实验> 研究,关心 <方法A> 对 <对象B> 的 <问题C>;
偏 <方法/可观测量/…>,不太关心 <明确排除的方向>。 -->
